<?xml version="1.0" encoding="utf-8"?>
<!--
    Tema minimo: solo lo que se ve mientras la app arranca, antes de que
    Compose pinte la primera pantalla. El aspecto real lo define
    NxTimeTheme (ui/theme/Theme.kt).

    "DayNight" hace que el fondo de arranque siga al tema del sistema,
    tomando el color de values-night/colors.xml. Antes el tema base
    fijaba windowLightStatusBar a true para toda la app, asi que en modo
    oscuro los iconos de la barra de estado quedaban oscuros sobre fondo
    oscuro; ahora eso lo ajusta NxTimeTheme segun el tema activo.
-->
<resources>
    <style name="Theme.NxTime" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
        <item name="android:navigationBarColor">@color/window_background</item>
    </style>
</resources>
